# Qwen Image 2.1

Qwenの公式Diffusersパイプラインを専用環境で実行する画像生成・編集タブです。Neo本体の依存関係を変更せず、既存のQwen-Imageモデルと併用できます。

## 導入と使い方

1. Neoを終了し、ルートの`aikimi-qwen-image21-setup.bat`を実行します。Python 3.13、Git、NVIDIA GPUが必要です。
2. 公式モデル一式約33GBと専用環境を準備します。環境の容量も含めて余裕を確保してください。同じBATを再実行すると取得済みファイルを再利用し、モデルのサイズとSHA-256を検証します。
3. 普段の`aikimi-launch.bat`で起動し、上部の**Qwen Image 2.1**を開きます。
4. プロンプトを入力し、最初は**INT8・CPU退避・1024×1024・40 steps**で生成します。Seedが`-1`なら毎回ランダムです。実際に使ったSeedは結果と一緒に保存します。

参照なしで新規生成、参照を追加すると画像編集になります。最大10枚を順番に指定できます。画像内の人物や物を指すときは「1枚目」「2枚目」のようにプロンプトに書いてください。透過背景を有効にすると公式の透過指示をプロンプトへ加え、生成したアルファチャンネルをPNGへ保存します。背景が確実に透明になることを保証する後処理ではありません。

結果は画面から保存し、そのまま次の編集の参照にも使えます。保存先は`outputs/qwen-image-2.1/`です。各生成の`request.json`、`result.json`、`output.png`、`worker.log`に設定・画像・実行記録が残ります。停止はその画面で開始したジョブにだけ作用します。ブラウザーを閉じてもジョブは継続し、実プロセスの停止確認が終わるまでGPU使用権を保持します。

## INT8とメモリ設定

| 設定 | 動作 |
|---|---|
| INT8 | 生成TransformerとQwen3-VLテキストエンコーダーの対応Linear層をbitsandbytes LLM.int8()で読み込みます。小さな入出力・正規化などの層とVAEは浮動小数点のままです。bitsandbytes内部では入力をFP16へ変換するため、全演算がBF16になる設定ではありません。 |
| BF16 | 公式のBF16構成を読み込みます。INT8より多くのメモリを使います。 |
| CPU退避 | 各部品を使うタイミングでGPUへ転送します。CPU RAMと転送時間を使ってVRAM使用量を抑えます。 |
| GPU常駐 | パイプライン全体をGPUへ置きます。十分なVRAMがある場合に選んでください。 |

INT8は公式BF16重みを読み込む際に量子化するため、ダウンロード容量は減りません。初回の読み込みには量子化処理が加わります。同じ精度・メモリ設定では、Neo共通の「GPU・モデル保持」に従ってモデルを再利用します。設定を変えると読み直します。bitsandbytes 0.50.2の内部INT8データが親モデルのCPU退避に追従しないケースは、このworker内で補正しています。

**ここでのINT8はbitsandbytes形式です。Comfy-Org配布のINT8 ConvRotや従来のForge用Qwen単体チェックポイントは、このタブでは読み込めません。** 量子化後の画質・速度・メモリ使用量は設定に依存します。BF16と同じ画像になるとは限りません。

1024角は試作向けの初期設定です。公式の2Kサイズも選べます。大きな画像や多数の参照はVRAMを多く使うため、参照を少なくして試してください。参照画像は公式パイプラインで約1MPを基準に前処理します。マスク専用入力、denoise strength、ForgeのLoRA・ControlNet・追加スクリプトはこのタブには接続していません。

## 固定版と検証

- モデル: `Qwen/Qwen-Image-2.1`、revision `b3179ad355be050328e483a9dfdd9e60cd62adfa`
- Diffusers: `6256aa7666cedd47443adc8f82da9a10e110b09c`（Qwen Image 2.1対応の公式コミット）
- Transformers 5.17.0、bitsandbytes 0.50.2、Accelerate 1.15.0
- PyTorch 2.11.0 / CUDA 13.0。対応するNVIDIAドライバーとBF16対応GPUが必要です。

導入内容の表示だけなら`aikimi-qwen-image21-setup.bat --dry-run`、環境だけの準備は`--runtime-only`、モデルの完全性の再検証は`--verify`を付けて実行します。生成時にはモデルを自動取得しません。不足があればセットアップ方法を表示します。

2026年9月21日、Windows 11 / Python 3.13.14で全体のCPU・オフラインテスト1,452件が完了（48件スキップ、既知の期待失敗1件）。YuE2の別スイートは44件成功・5件スキップ。公式コードの小型モデルを使うCPUテスト3件と、RTX 3090で両モデルの実INT8読み込み・GPU常駐・CPU offload・2回連続実行を調べる追加テストも成功しました。小型モデルはランダム重みであり、画質評価ではありません。

実Chromeでは専用タブへの切り替え、設定表示、入力エラー、透過PNG2枚のアップロードと並べ替え、画面幅を狭めたときの配置を確認しました。アップロード後もRGBA値が一致しています。PNG・JSONの保存欄は、実Gradio・Chromiumで2回連続生成し、両ファイルの取得まで追加検証しています。

RTX 3090（24GB）で、公式モデルのINT8新規生成と参照1枚の編集を、1024×1024・40 steps・CPU退避で確認しました。Transformer 224層とテキストエンコーダー368層がINT8で、出力は1024×1024のRGBA PNGです。初回モデル読み込み約299秒、生成処理約70秒、モデルを再利用した参照編集約78秒でした。2048×2048・40 stepsの参照編集も完了し、猫を保ったままカップを青へ変更できました。2Kの処理時間は約564秒で、最後の画像変換ではGPUメモリ使用量が約23,400MiBに達しています（デスクトップ等を含む観測値）。BF16も1024×1024・4 stepsで読み込み・生成・PNG保存を確認しました。4 stepsの確認は画質評価ではありません。システムRAMは64GBで、BF16の初回は読み込みとGPUへの転送を含め約873秒かかりました。これらはこの環境での単発の動作確認値です。

## 出典と利用条件

2026年9月20日に[公式リポジトリ](https://github.com/QwenLM/Qwen-Image-2.1)、[公式モデル](https://huggingface.co/Qwen/Qwen-Image-2.1)、[Diffusersの対応PR](https://github.com/huggingface/diffusers/pull/14804)を確認しました。

モデルには[Qwen Research License](https://huggingface.co/Qwen/Qwen-Image-2.1/blob/b3179ad355be050328e483a9dfdd9e60cd62adfa/LICENSE)が適用されます。公開時の条項は研究・評価目的の非商用利用に限定しており、商用利用には別途ライセンスが必要です。DiffusersのコードはApache-2.0です。モデル重みはこのGitリポジトリには含めません。
